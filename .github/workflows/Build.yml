name: Build KeyDrive

on:
  pull_request:
  push:
    branches:
      - main
  release:
    types:
      - created


jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-20.04
    steps:
      - run: yarn --version
      - run: go version
      - run: docker --version
